const { Client, Message, ChannelType } = require('discord.js');
const db = require('./dbConfiguration.js');
/**
 * 
 * @param {Client} client 
 * @param {Message} message 
 */
const modmailActivity = async(client, message) => {

    // Coming in v1.2

};